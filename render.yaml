services:
  - type: web
    name: cpu-duel-arena
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python online_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.7
